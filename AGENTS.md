# Repository Agent Instructions

Last verified: 2026-07-17.

Before modifying any file in this repository, read
`skills/maintain-lerobot-converter/SKILL.md` and every reference it routes for the affected area.
These instructions apply to code, tests, UI, configuration, installation, operations, and docs.

The repository skill is live documentation. If a change makes any skill statement stale or
misleading, update the affected skill/reference in the same change, remove obsolete wording, and set
its `Last verified` date to the current date. Never leave future agents with contradictory guidance.

Preserve unrelated worktree changes. Verify behavior before committing, and never commit runtime
state, credentials, generated datasets, conversion caches, dependency directories, or virtual
environments.
